1. 4N + 16 steps in this case means the algorithm's steps are multiplied by four, and 16 is added to it afterward, making the expression follow a linear trend. Based on this, the time complexity would be O(n).

2. The time complexity would be O(n^2) since O(2n^2) follows the trend of n^2 complexity. 

3. Since each loop goes through the array once, it will follow O(2n) time complexity since there are two loops doing one task. Overall, it will be in O(n) time complexity.

4. O(n). Every input gives three outputs and follows a linear trend. The best case would be strings that cannot be uppercase, lowercase, or capitalized. (Like numbers & symbols.)

5. Best case is O(n), where each input is odd. The worst case is O(n^2) when each input is even.
